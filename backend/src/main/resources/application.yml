server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kddizayn
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: never   # data.sql'i ilk kurulumda always yapın

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Set MAIL_HOST, MAIL_PORT, MAIL_USERNAME and MAIL_PASSWORD in production.
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:kddizaynmimarlik@gmail.com}
    password: ${MAIL_PASSWORD:drqa pdbn wtqo cvoi}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    test-connection: false

app:
  jwt:
    secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
    expiration-ms: 86400000        # 24 saat
    refresh-expiration-ms: 604800000  # 7 gün
  upload:
    dir: /Users/mys/Documents/programming/projects/kd/uploads
  cors:
    allowed-origins: "http://localhost:3001,https://kddizayn.com"
